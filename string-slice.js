const anthem = "Amar shonar bangla ami tomai valobashi";
const words = anthem.split(" ");
const slice = anthem.slice(5,13);
const subStr = anthem.substr(5,3);

const first = 'amari';
const second = 'Dhaka';

const fullString = first.concat(second).concat('Khulna');

const words2 = ['a', 'b', 'c', 'd', 'e', 'f', 'g'];
const allJoined = words2.join('');
console.log(allJoined);